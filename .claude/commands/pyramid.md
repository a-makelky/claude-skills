---
description: "Build a Minto Pyramid and restructuring plan for a draft or idea."
argument-hint: "[draft, idea, or blank to use recent context]"
---

Use the `minto-pyramid` skill for this request.

Input:

```text
$ARGUMENTS
```

If the input is blank, use the most recent obvious draft or topic in the conversation. Create the skill's HTML artifact and reply only with the file link plus a two to four line summary.
